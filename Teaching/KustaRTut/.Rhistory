summarise_all(ONP, funs(n_distinct)) %>% select_if(., function(x) x > 2 & x < 50) %>% colnames()
ex3 <- summarise_all(ONP, funs(n_distinct)) %>% select_if(., function(x) x > 2 & x < 50) %>% colnames()
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>% select_if(., function(x) x > 2 & x < 50) %>% colnames()
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x < 50) %>%
colnames()
select(ONP, ex3.cols)
select(ONP, ex3.cols) %>% hist()
select(ONP, ex3.cols) %>% ex3
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x < 50) %>%
colnames()
par()
ex3 <- select(ONP, ex3.cols)
ex3
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x < 50) %>%
colnames()
par()
nc <- ncol(ex3)
nc
par(mfrow=c(4, 3))
par(mfrow = c(4, 3))
sapply(ex3, hist)
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x < 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(4, 3))
sapply(ex3, hist)
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
sapply(ex3, hist)
sapply(ex3, hist, main = names(ex3))
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
sapply(ex3, hist, main = names(ex3))
sapply(ex3, function(x) hist(x, main = names(x)))
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
sapply(ex3, function(x) hist(x, main = names(x)))
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
sapply(ex3, hist)
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
sapply(ex3, hist)
sapply(ex3.cols, main)
sapply(ex3.cols, title)
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
sapply(ex3, hist)
sapply(ex3.cols, title)
apply(ex3.cols, title)
sapply(ex3.cols, title)
sapply(ex3, hist, main = ex3.cols[i])
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
sapply(ex3, hist, main = ex3.cols[i])
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
sapply(ex3, hist)
ex3[[1]]
ex3
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
sapply(ex3, hist)
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[,i], main = ex3.cols[i])
}
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[,..i], main = ex3.cols[i])
}
ex3[..1]
ex3[,..1]
ex3[,..2]
ex3[,.1]
ex3[,.2]
ex3[,1]
ex3[,3]
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[[i]], main = ex3.cols[i])
}
tr <- sample_n(ONP, nrow(ONP)*.5)
View(ONP)
summarise_all(ONP, funs(n_distinct))
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[[i]], main = ex3.cols[i])
}
tr <- sample_n(ONP, nrow(ONP)*.5)
ts <- ONP[.(),]
ts[,url]
tr <- sample_n(ONP, nrow(ONP)*.5)
tr
ts[,url]
ts[,.url]
ts[url]
ts
trn <- sample_n(ONP, nrow(ONP)*.5)
trn[,url]
ONP[! url %in% trn[,url],]
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[[i]], main = ex3.cols[i])
}
# ex.4 --------------------------------------------------------------------
trn <- sample_n(ONP, nrow(ONP)*.5)
tst <- ONP[! url %in% trn[,url],]
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[[i]], main = ex3.cols[i])
}
# ex.4 --------------------------------------------------------------------
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[! url %in% trn[,url],]
tst <- trn[,url] %>% ONP[!url %in% .,]
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[[i]], main = ex3.cols[i])
}
# ex.4 --------------------------------------------------------------------
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- trn[,url] %>% ONP[!url %in% .,]
tst <- trn[,url] %>% ONP[!url %in% ..,]
trn[,url]
tst <- trn[,url] %>% ONP[!url %in% .,]
tst <- trn[,url] %>% ONP[!url %in% (.),]
tst <- trn[,url] %>% ONP[!url %in% ,]
tst <- trn[,url] %>% ONP[!url %in% .,]
trn[,url] %>% ONP[!url %in% .,]
ONP[!url %in% .,]
a%>%ONP[.,]
url%>%ONP[.,]
url %> %ONP[.,]
url %>% ONP[.,]
url %>% ONP[,.]
url %>% ONP[,...]
url %>% ONP[,..]
group_by(trn, 1:5)
sample(1:5, nrow(trn), replace = T)
sample(1:5, nrow(trn), replace = T) %>% group_by(trn, .)
sample(1:5, nrow(trn), replace = T) %>% group_by(trn, (.))
group_by(trn, sample(1:5, nrow(trn), replace = T))
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T))
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>% group_by(trn, cvid)
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[!url %in% trn[,url], ]
sample(1:5, nrow(trn), replace = T) %>% group_by(trn, .)
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>% group_by(cvid)
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., mean)
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., mean(shares))
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., mean(shares))
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., n())
trn[cvid:=sample(1:5, nrow(trn)]
trn[,cvid:=sample(1:5, nrow(trn)]
sample(1:5, nrow(trn)
trn[,cvid:=sample(1:5, nrow(trn))]
trn[,.(cvid := sample(1:5, nrow(trn)))]
trn[,.(cvid = sample(1:5, nrow(trn)))]
trn[,.(cvid = sample(1:5, nrow(trn), replace = T))]
trn[,.(cvid := sample(1:5, nrow(trn), replace = T))]
trn[,.(cvid = sample(1:5, nrow(trn), replace = T))]
trn[,.(cvid := sample(1:5, nrow(trn), replace = T))]
trn[,.('cvid' := sample(1:5, nrow(trn), replace = T))]
trn[,cvid := sample(1:5, nrow(trn), replace = T)]
?data.table
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[[i]], main = ex3.cols[i])
}
# ex.4 --------------------------------------------------------------------
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[!url %in% trn[,url], ]
sample(1:5, nrow(trn), replace = T) %>% group_by(trn, .)
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., n())
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
View(trn)
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[!url %in% trn[,url], ]
sample(1:5, nrow(trn), replace = T) %>% group_by(trn, .)
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., n())
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[!url %in% trn[,url], ]
sample(1:5, nrow(trn), replace = T) %>% group_by(trn, .)
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[!url %in% trn[,url], ]
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., n())
trn[,cvid := sample(1:5, nrow(trn), replace = T),]
trn[,cvid := sample(1:5, nrow(trn), replace = T)]
trn[,cvid := sample(1:5, nrow(trn), replace = T)]
trn[,cvid := sample(1:5, nrow(trn), replace = T)]
trn[,cvid := sample(1:5, nrow(trn), replace = T)]
trn[,cvid := sample(1:5, nrow(trn), replace = T)]
trn[,cvid := sample(1:5, nrow(trn), replace = T)]
View(trn)
View(trn)
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[!url %in% trn[,url], ]
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., n())
trn[, cvid := sample(1:5, nrow(trn), replace = T)]
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[[i]], main = ex3.cols[i])
}
# ex.4 --------------------------------------------------------------------
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[!url %in% trn[,url], ]
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., n())
trn[, cvid := sample(1:5, nrow(trn), replace = T)]
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[[i]], main = ex3.cols[i])
}
# ex.4 --------------------------------------------------------------------
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[!url %in% trn[,url], ]
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., n())
trn[, cvid := sample(1:5, nrow(trn), replace = T)]
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
# ex.3 --------------------------------------------------------------------
ex3.cols <- summarise_all(ONP, funs(n_distinct)) %>%
select_if(., function(x) x > 2 & x <= 50) %>%
colnames()
ex3 <- select(ONP, ex3.cols)
nc <- ncol(ex3)
par(mfrow = c(3, 3))
for (i in 1:nc) {
hist(ex3[[i]], main = ex3.cols[i])
}
# ex.4 --------------------------------------------------------------------
trn <- sample_n(ONP, nrow(ONP)*.7)
tst <- ONP[!url %in% trn[,url], ]
mutate(trn, cvid = sample(1:5, nrow(trn), replace = T)) %>%
group_by(cvid) %>% summarise(., n())
trn[, cvid := sample(1:5, nrow(trn), replace = T)]
x <- seq(.1, 3, length.out = 1e2)
x <- seq(.1, 3, length.out = 1e2)
y <- -2+1/2+x
plot(x, y)
x <- seq(.1, 3, length.out = 1e2)
y <- -2+1/2+x
plot(x, y)
x <- seq(.1, 3, length.out = 1e2)
x <- seq(.1, 3, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
x <- seq(.1, 1, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
x <- seq(1e-2, 1, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
x <- seq(.1, 1, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
x <- seq(.1, 3, length.out = 1e2)
x <- seq(-1.9, 3, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
x <- seq(-1, 3, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
x <- seq(-1.5, 3, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
x <- seq(-1.5, 3, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
x <- seq(-1.8, 3, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
x <- seq(-1.8, 0, length.out = 1e2)
y <- -2+1/(2+x)
plot(x, y)
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
require(data.table)
require(dplyr)
ONP <- fread('./OnlineNewsPopularity.csv')
require(data.table)
require(dplyr)
ONP <- fread('./data/OnlineNewsPopularity.csv')
summarise_all(ONP, funs(n_distinct))
onp_col <- colnames(ONP)
for (i in 1:length(onp_col)) { distinct(ONP,ONP$onp_col[i])}
?distinct
onp_col[1]
ONP$url
ONP$dsa
ONP$'dsa'
ONP[url]
